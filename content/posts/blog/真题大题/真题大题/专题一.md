---
title: "真题大题-专题一"
date: 2025-12-30
summary: "408真题专题一"
categories:
  - "技术"
tags:
  - "408"
  - "考研"
---

# 专题一

> 原文: https://app.notion.com/p/2accd32c4e88818fa149fb4141b118fb

1. ★★★【2011年统考真题】某银行提供1个服务窗口和10个供顾客等待的座位。顾客到达银行时，若有空座位，则到取号机上领取一个号码，等待叫号。取号机每次仅允许一位顾客使用。当营业员空闲时，通过叫号选取一位顾客，并为其服务。顾客和营业员的活动过程描述如下:
```c++
cobegin
{
		process 顾客 i
		{
				从取号机获取一个号码；
				等待叫号；
				获取服务；
		}
		
		process 营业员
		{
				While(TURE)
				{
						叫号；
						为客户服务；
				}
		}

} coend
```

请添加必要的信号量和 P、V［或 wait()、signal()］操作，实现上述过程中的互斥与同步。要求写出完整的过程，说明信号量的含义并赋初值。

```c
// 设置信号量
semaphore mutex = 1; // 取号机互斥访问
semaphore full = 0; // 当前等待的人数
semaphore empty = 10; // 空位数量
semaphore service = 0; // 营业员是否空闲
```

![](https://prod-files-secure.s3.us-west-2.amazonaws.com/ca267836-1b7e-4706-90e5-67785744a0c7/ab170b3e-448b-49b5-821e-7782a506addd/image.png?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=ASIAZI2LB466QKIODYRI%2F20260619%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20260619T120400Z&X-Amz-Expires=3600&X-Amz-Security-Token=IQoJb3JpZ2luX2VjEPP%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLXdlc3QtMiJHMEUCIFnY0xpMWs52c4fic3tIVkl7jXC7Z0yLdva%2F3xMJtBWmAiEAiLAusNczh2fm7BdR2iViyJtaaQxu2uFEZWI1%2Bo2pwhYqiAQIvP%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FARAAGgw2Mzc0MjMxODM4MDUiDB8OtJY6p8P7JhaeHCrcA%2FZLKfPMfI8sTNIXHDd4sf7hXLslVlgZ8X%2F2X6yZdjWY4z4Z475R3SaaKiT6CZbY4q9A8PCkMVLjC6tc%2Baebc6y0ynyfxJG3rtI%2FvdPxOJBNgStYu6is%2B3j9hgkBh3Sv7UB76j5fd50pLBMRG6cT7VEH4Nxg%2BMzTm5rLh10Qh1Xc7xlt%2BEbjkWk%2BfgPFGdkkO9JWT0uu%2BGBhNsVJhrmAYuI%2BvvEIt3f5BafFZuCEF8ocvJjNqnodq9uIhxqg%2BQwSgF4Wure2eagOo%2Boc6dt18IiGAtcy%2BsLuCpgWkIB%2Fm9bM4VdeM6PtgKsK9%2FYM%2BUuvYnw12SH4Oe96w9Qnw%2BlRs3fjhv6X9ImBQ9zWhUdKxcHP1cESiRhbkP6MsrB8j4%2Bsh6ucVSBSCNMzKSnk3HO0AkhwDCx95ROAt7HaxhN2vNZ1VSaN68rQ3SAfInU54hlbMeRyBnrcDcNAAhzGGZPuUkETaBjt1ofyNU6PyhGt%2FAlpYVDN3DTarnR%2BwpDmU9pv%2BY%2FlTFZofQekNVre%2FMMqEgpB6CrTaKAxr1EUdw%2BoXd6mov4Vo2zzMXGm3ecciFq%2Be64N%2Bc2hUd6SmkrTkgRNYXonYj9XIE%2BosNU6rhEBsFqJFgQqIVEIoK3QMuUWMMTA1NEGOqUBnxbi2QHJFT1CCM2tVnHrLN1S%2FbeXdr9iHXv%2F2fYj04Vt9uMcvOegau7OfKHVQAB5rYOsDNTxUgUG7vjj4GOb2JitKfkHjuZZ9L7h3nrYZuKbgDhLIAuBYk51B2Wdvt205cx%2FTNqs5hJcA%2FB6783%2B6BIOtwTbmHMBrN3rBnxdRimjK9bsRc6tjkx6sb%2Bg8y1tAGN9%2BeTGKDlc2YZ5ifU0D%2BTxoltd&X-Amz-Signature=d582364dd99eb1500e191d124146bbde08f8ecb7eafb55ffaec5f7c2dfc9e181&X-Amz-SignedHeaders=host&x-amz-checksum-mode=ENABLED&x-id=GetObject)

```c
cobegin
		semaphore mutex = 1;
		semaphore empty = 500;
		
		参观者进程 i;
		{
				P(empty);
				P(mutex);
				进门;
				V(mutex);
				参观;
				P(mutex);
				出门;
				V(mutex);
				V(empty);		 
		}
coend
```


<mark style="background-color: #f4dfeb;">错题</mark>

1. ★★【2014年统考真题】系统中有多个生产者进程和多个消费者进程，共享一个能存放1000件产品的<mark style="background-color: #ddebf1;">环形缓冲区</mark>（初始为空）。缓冲区未满时，生产者进程可以放入其生产的一件产品，否则等待；缓冲区未空时，消费者进程可以从缓冲区取走一件产品，否则等待。要求一个消费者进程从缓冲区连续取出10件产品后，其他消费者进程才可以取产品。请使用信号`P`、`V`（`wait()`、`signal()`）操作实现进程间的互斥与同步，要求写出完整的过程，并说明所用信号量的含义和初值。
```c
// 共享信号量（初值如上）
semaphore empty = 1000;
semaphore full  = 0;
semaphore mutex = 1;
semaphore c_lock = 1;

// 环形缓冲区与指针
item buffer[1000];
int in = 0, out = 0;

/*************** 生产者进程 ****************/
producer() {
    while (true) {
        item x = produce();        // 生产一个产品（本地操作）

        P(empty);                  // 需要一个空位
        P(mutex);                  // 进入缓冲区临界区
            buffer[in] = x;
            in = (in + 1) % 1000;
        V(mutex);                  // 离开临界区
        V(full);                   // 增加可用产品数
    }
}

/*************** 消费者进程 ****************/
consumer() {
    while (true) {
        // 申请批量锁，获得“本轮取10件”的资格
        P(c_lock);

        // 连续取满10件
        for (int i = 0; i < 10; ++i) {
            P(full);               // 等待至少有1件产品
            P(mutex);              // 进入缓冲区临界区
                item x = buffer[out];
                out = (out + 1) % 1000;
            V(mutex);              // 离开临界区
            V(empty);              // 归还一个空位

            consume(x);            // 消费该产品（本地操作）
        }

        // 释放批量锁，允许下一个消费者开始它的“10件批次”
        V(c_lock);
    }
}
```


1. ★★【2015年统考真题】有A、B两人通过信箱进行辩论。每个人都从自己的信箱中取得对方的问题，将答案和向对方提出的新问题组成一个邮件放入对方的邮箱中。假设A的信箱最多放M个邮件，B的信箱最多放N个邮件。初始时A的信箱中有x个邮件（`0≤x<M`），B的信箱中有y个邮件（`0≤y<N`）。辩论者每取出一个邮件，邮件数减1。A和B两人的操作过程描述如下:
  ---

  CoBegin

  CoEnd

  ---

  当信箱不为空时，辩论者才能从信箱中取邮件，否则等待。当信箱不满时，辩论者才能将新邮件放入信箱，否则等待。请添加必要的信号量`P`、`V`（或`wait()`、`signal()`）操作，以实现上述过程的同步。要求写出完整的过程，并说明信号量的含义和初值。

```c
// A 信箱
semaphore mutexA = 1, fullA = x, emptyA = M - x;
// B 信箱
semaphore mutexB = 1, fullB = y, emptyB = N - y;
```



1. ★★【2016年统考真题】某个进程调度程序采用基于<mark style="background-color: #f4dfeb;">优先数（priority）</mark>的调度策略，即选择优先数最小的进程运行。进程创建时由用户指定一个 `nice` 作为静态优先数。为了动态调整优先数，引入运行时间 cpuTime 和等待时间 waitTime，初值均为 0。进程处于执行态时，cpuTime 定时加 1，且 waitTime 置 0；进程处于就绪态时，cpuTime 置 0，waitTime 定时加 1。请回答下列问题:
  (1) 若调度程序只将 nice 的值作为进程的优先数，即 priority = nice，则可能会出现饥饿现象。为什么？

  nice 作为静态优先数，当不断有优先数较小的进程加入就绪队列时，nice 值较大的进程会迟迟得不到 CPU 资源造成饥饿现象。


  (2) <span style="color: #e03e3e;">使用 nice、cpuTime 和 waitTime 设计一种动态优先数计算方法，以避免产生饥饿现象，并说明 waitTime 的作用。</span>

  ```c
priority = nice + cpuTime - waitTime;
  ```

  waitTime的作用在于调节就绪进程的等待时间，根据等待时间适当提高优先级。


1. ★★★【2017年统考真题】某进程中有3个并发执行的线程 thread1、thread2 和 thread3，其伪代码如下所示。
请添加必要的信号量和 P、V［或 `wait()`、`signal()`］操作，要求确保线程互斥访问临界资源，并且最大限度地并发执行。

```c
semaphore y3w = 1;
semaphore y1r = 1, y2r = 1;
semaphore zrw = 1;
```


1. ★★★【2019年统考真题】有 n（n≥3）名哲学家围坐在一张圆桌边，每名哲学家交替地就餐和思考。在圆桌中心有 m（m≥1）个碗，每两名哲学家之间有一根筷子。每名哲学家必须取到一个碗和两侧的筷子后，才能就餐，进餐完毕，将碗和筷子放回原位，并继续思考。为使尽可能多的哲学家同时就餐，且防止出现死锁现象，请使用信号量的 P、V 操作［`wait()`、`signal()`］描述上述过程中的互斥与同步，并说明所用信号量及初值的含义。
```c++
// 奇偶交错 + 碗数

// 哲学家 i (1 ≤ i ≤ n)
while (true) {
    P(bowl);   // 先拿一个碗
    
    if (i % 2 == 0) {  // 偶数号:先左后右
        P(chopsticks[i - 1]);           // 左筷子，下标 i-1
        P(chopsticks[i % n]);           // 右筷子，下标 i%n
    } else {            // 奇数号:先右后左
        P(chopsticks[i % n]);           // 右筷子
        P(chopsticks[i - 1]);           // 左筷子
    }

    就餐();

    V(chopsticks[i % n]);   // 放下右筷子
    V(chopsticks[i - 1]);   // 放下左筷子
    V(bowl);                // 放回碗

    思考();
}
```

<details><summary>Python 模拟</summary>

```c++
import threading
import time
import random

# --- 模拟参数 ---
N_PHILOSOPHERS = 5  # n: 哲学家数量
M_BOWLS = 3         # m: 碗的数量
SIMULATION_DURATION = 10 # 模拟持续时间（秒）

# --- 辅助函数 ---
def think(philosopher_id):
    """模拟思考"""
    think_time = random.uniform(0.01, 0.1)
    # print(f"哲学家 {philosopher_id} 正在思考...")
    time.sleep(think_time)

def eat(philosopher_id, meal_counter):
    """模拟就餐并计数"""
    eat_time = random.uniform(0.01, 0.1)
    # print(f"哲学家 {philosopher_id} 正在就餐。")
    time.sleep(eat_time)
    # 使用锁来安全地增加全局计数器
    with meal_counter[1]:
        meal_counter[0] += 1

# --- 算法1: 全局互斥锁 ---
def philosopher_mutex(p_id, chopsticks, bowl, mutex, meal_counter, stop_event):
    while not stop_event.is_set():
        think(p_id)
        
        mutex.acquire()
        bowl.acquire()
        chopsticks[p_id].acquire()            # 左手筷子
        chopsticks[(p_id + 1) % N_PHILOSOPHERS].acquire() # 右手筷子
        mutex.release() # 获取完资源后应立即释放全局锁，让其他人可以吃饭
        
        eat(p_id, meal_counter)
        
        chopsticks[(p_id + 1) % N_PHILOSOPHERS].release()
        chopsticks[p_id].release()
        bowl.release()

# --- 算法2: 房间法 ---
def philosopher_room(p_id, chopsticks, bowl, room, meal_counter, stop_event):
    while not stop_event.is_set():
        think(p_id)
        
        room.acquire()
        bowl.acquire()
        chopsticks[p_id].acquire()
        chopsticks[(p_id + 1) % N_PHILOSOPHERS].acquire()
        room.release() # 获取成功，立刻离开房间，让别人能进来尝试
        
        eat(p_id, meal_counter)
        
        chopsticks[(p_id + 1) % N_PHILOSOPHERS].release()
        chopsticks[p_id].release()
        bowl.release()

# --- 算法3: 奇偶交错法 ---
def philosopher_odd_even(p_id, chopsticks, bowl, meal_counter, stop_event):
    while not stop_event.is_set():
        think(p_id)
        
        bowl.acquire()
        if p_id % 2 == 0: # 偶数号: 先左后右
            chopsticks[p_id].acquire()
            chopsticks[(p_id + 1) % N_PHILOSOPHERS].acquire()
        else: # 奇数号: 先右后左
            chopsticks[(p_id + 1) % N_PHILOSOPHERS].acquire()
            chopsticks[p_id].acquire()
            
        eat(p_id, meal_counter)
        
        chopsticks[p_id].release()
        chopsticks[(p_id + 1) % N_PHILOSOPHERS].release()
        bowl.release()

def run_simulation(philosopher_logic, n, m, description):
    """通用模拟运行器"""
    chopsticks = [threading.Semaphore(1) for _ in range(n)]
    bowl = threading.Semaphore(m)
    meal_counter = [0, threading.Lock()] # [count, lock]
    stop_event = threading.Event()
    
    # 根据不同算法设置额外信号量
    extra_semaphores = {}
    if philosopher_logic == philosopher_mutex:
        extra_semaphores['mutex'] = threading.Semaphore(1)
    elif philosopher_logic == philosopher_room:
        # 房间容量不能超过哲学家总数-1
        room_size = min(n - 1, m * 2) # 一个更优化的想法，房间容量也受碗数限制
        extra_semaphores['room'] = threading.Semaphore(room_size)

    threads = []
    for i in range(n):
        thread = threading.Thread(target=philosopher_logic, args=(i, chopsticks, bowl, *extra_semaphores.values(), meal_counter, stop_event))
        threads.append(thread)
        thread.start()
        
    print(f"\n--- 开始模拟: {description} ---")
    print(f"哲学家数量: {n}, 碗数量: {m}")
    time.sleep(SIMULATION_DURATION)
    
    stop_event.set()
    for thread in threads:
        thread.join()
        
    print(f"--- 模拟结束 ---")
    print(f"在 {SIMULATION_DURATION} 秒内, 总共就餐次数: {meal_counter[0]}")
    return meal_counter[0]

if __name__ == "__main__":
    print("="*40)
    print("开始并发性测试...")
    print("="*40)
    
    results = {}
    
    # 运行算法1
    results['全局互斥锁'] = run_simulation(philosopher_mutex, N_PHILOSOPHERS, M_BOWLS, "算法1: 全局互斥锁")
    
    # 运行算法2
    results['房间法'] = run_simulation(philosopher_room, N_PHILOSOPHERS, M_BOWLS, "算法2: 房间法")

    # 运行算法3
    results['奇偶交错法'] = run_simulation(philosopher_odd_even, N_PHILOSOPHERS, M_BOWLS, "算法3: 奇偶交错法")
    
    print("\n\n" + "="*40)
    print("           最终结果对比")
    print("="*40)
    for name, count in results.items():
        print(f"{name}:\t{count} 次就餐")
    print("="*40)
```

</details>

<details><summary>并发性测试结果对比</summary>

```c++
--- 开始模拟: 算法1: 全局互斥锁 ---
哲学家数量: 5, 碗数量: 3
--- 模拟结束 ---
在 10 秒内, 总共就餐次数: 261

--- 开始模拟: 算法2: 房间法 ---
哲学家数量: 5, 碗数量: 3
--- 模拟结束 ---
在 10 秒内, 总共就餐次数: 288

--- 开始模拟: 算法3: 奇偶交错法 ---
哲学家数量: 5, 碗数量: 3
--- 模拟结束 ---
在 10 秒内, 总共就餐次数: 300


========================================
           最终结果对比
========================================
全局互斥锁:     261 次就餐
房间法:        288 次就餐
奇偶交错法:     300 次就餐
========================================
```

</details>

